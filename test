#!/bin/bash
for validinvalid in ./TEST_PROGRAMS/valid/*; do
	# for subfolder in $validinvalid/*; do
        for filename in $validinvalid/*; do 
        echo "$filename" 
        ./src/goLite.native $filename 
        done
	# done
done
        
# done
